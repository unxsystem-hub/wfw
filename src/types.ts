export interface ImagePart { data: string; mimeType?: string; }
export interface CinematicDetails { aspectRatio?: string; quality?: string; }
export interface EditedImageResult { newImage: string; responseText?: string; }